<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korit.ch03.mapper.UserMapper">

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into users values
            (default, #{username}, #{password}, #{name}, #{email}, now(), now())
    </insert>

    <select id="selectAll" resultType="User">
        select
            id,
            username,
            password,
            name,
            email,
            created_at,
            updated_at
        from
            users
        order by
            id
    </select>

    <select id="selectById" resultType="User">
        select
            id,
            username,
            password,
            name,
            email,
            created_at,
            updated_at
        from
            users
        where
            id = #{userId}
        order by
            id
    </select>

    <select id="selectByUsername" resultType="User">
        select
            id,
            username,
            password,
            name,
            email,
            created_at,
            updated_at
        from
            users
        where
            username = #{username}
        order by
            id
    </select>

</mapper>